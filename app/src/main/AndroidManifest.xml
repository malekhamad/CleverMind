<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    package="com.geniusmind.malek.clevermind">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen.LatestNewsActivity"

            ></activity>
        <activity
            android:name=".HomeScreen.TrainingCenterActivity"
        />
        <activity
            android:name=".HomeScreen.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Splash_Login_Register.SplashActivity"
            app:background="@drawable/background">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash_Login_Register.LoginActivity" />
        <activity
            android:name=".Splash_Login_Register.SignUpActivity"
            android:parentActivityName=".Splash_Login_Register.LoginActivity" />
    </application>

</manifest>